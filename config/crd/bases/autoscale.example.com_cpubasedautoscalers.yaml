---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: cpubasedautoscalers.autoscale.example.com
spec:
  group: autoscale.example.com
  names:
    categories:
    - autoscaling
    kind: CPUBasedAutoscaler
    listKind: CPUBasedAutoscalerList
    plural: cpubasedautoscalers
    shortNames:
    - cpuba
    singular: cpubasedautoscaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.kind
      name: Target
      type: string
    - jsonPath: .spec.targetRef.name
      name: Name
      type: string
    - jsonPath: .status.cpuSmoothPercent
      name: CPU(smooth)%
      priority: 1
      type: string
    - jsonPath: .status.currentReplicas
      name: Cur
      type: integer
    - jsonPath: .status.desiredReplicas
      name: Des
      priority: 1
      type: integer
    - jsonPath: .status.appliedReplicas
      name: App
      priority: 1
      type: integer
    - jsonPath: .status.reason
      name: Reason
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CPUBasedAutoscaler is the Schema for the cpubasedautoscalers
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of CPUBasedAutoscaler
            properties:
              control:
                properties:
                  alpha:
                    default: 0.4
                    exclusiveMinimum: true
                    maximum: 1
                    minimum: 0
                    type: number
                  dtSeconds:
                    default: 15
                    format: int32
                    minimum: 1
                    type: integer
                  mode:
                    default: reactive
                    enum:
                    - reactive
                    type: string
                  targetCPUPercent:
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  warmupPoints:
                    default: 3
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - targetCPUPercent
                type: object
              policy:
                properties:
                  errorFreezeSeconds:
                    default: 60
                    format: int32
                    minimum: 0
                    type: integer
                  maxReplicas:
                    default: 50
                    format: int32
                    minimum: 1
                    type: integer
                  maxStepDown:
                    default: 3
                    format: int32
                    minimum: 0
                    type: integer
                  maxStepUp:
                    default: 5
                    format: int32
                    minimum: 0
                    type: integer
                  minReplicas:
                    default: 1
                    format: int32
                    minimum: 1
                    type: integer
                  stabilizationWindowSec:
                    default: 60
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              source:
                properties:
                  golectra:
                    description: Настройки Golectra.
                    properties:
                      baseURL:
                        description: http://golectra.default.svc:8080
                        minLength: 1
                        type: string
                      cpuKeyPrefix:
                        default: CPUutilization
                        minLength: 1
                        type: string
                      snapshotPath:
                        default: /api/v1/snapshot
                        minLength: 1
                        type: string
                      timeoutMs:
                        default: 800
                        format: int32
                        minimum: 1
                        type: integer
                    required:
                    - baseURL
                    type: object
                  type:
                    description: Тип источника метрик.
                    enum:
                    - golectra
                    - prometheus
                    type: string
                required:
                - type
                type: object
              targetRef:
                properties:
                  apiVersion:
                    minLength: 1
                    type: string
                  kind:
                    enum:
                    - Deployment
                    - StatefulSet
                    type: string
                  name:
                    minLength: 1
                    type: string
                  namespace:
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
            required:
            - control
            - policy
            - source
            - targetRef
            type: object
          status:
            description: status defines the observed state of CPUBasedAutoscaler
            properties:
              appliedReplicas:
                format: int32
                type: integer
              conditions:
                description: |-
                  conditions represent the current state of the CPUBasedAutoscaler resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              cpuNowPercent:
                type: number
              cpuSmoothPercent:
                type: number
              currentReplicas:
                format: int32
                type: integer
              desiredReplicas:
                format: int32
                type: integer
              lastError:
                type: string
              lastRun:
                format: date-time
                type: string
              reason:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
